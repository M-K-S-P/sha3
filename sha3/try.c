#include<stdio.h>
#include<stdlib.h>
#include<limits.h>
//#include "hard.c"


int main(){

  char S[5][5][fast_pow(2, l)]; // state
  for (int i = 0; i < sizeof(S); i++) {
    
    for (int j = 0; i < sizeof(sizeof(S)); i++) {
    
      for (int k = 0; i < sizeof(sizeof(sizeof(S))); i++) {
    
        S[i][j][k] = '\0';
    
      }
    
    }  
    
  }
  
}